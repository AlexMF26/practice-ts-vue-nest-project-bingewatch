services:
  app:
    command: [ "npm", "run", "${ENTRY}" ]
    user: ${TYPE}
    build:
      dockerfile: Dockerfile
      context: .
      target: "development-stage-${TYPE}-user"
      args:
        - UID=$UID
    volumes:
      - .:/app
    ports:
      # app port
      - 9000:9000
    networks:
      - bingewatch-dev-net

networks:
  bingewatch-dev-net:
    name: bingewatch-dev-net
